# Diagram Exports

These exported assets are lightweight placeholders until final SVG exports are prepared from the source drafts in `docs/visual-system/diagrams/`.

Use the existing filenames so pages can reference stable asset paths.
