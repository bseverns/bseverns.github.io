# Diagram Exports

- Authored diagram sources live in `docs/visual-system/diagrams/`.
- Put exported SVG or PNG files in `img/diagrams/exports/` when a page has a clear placement and size.
- Keep file names aligned with the source names where possible, for example `fleet-map.svg` from `fleet-map.md`.
