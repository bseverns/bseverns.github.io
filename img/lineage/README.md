# Lineage Images

Drop imported lineage images into the subdirectories here using the filenames listed in `docs/site-reset/asset-drop-points.md`.

These folders intentionally exist before the media arrives so the catalog and copy can point to stable future paths without exposing broken public links.
